apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: tomcat-blue
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tomcat-blue
    spec:
      containers:
        - name: tomcat-blue
          image: registry-ci.playpit.by/helloworld-apavarnitsyn:14
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
        