apiVersion: v1
kind: Service
metadata:
  name: tomcat-service-BUILD_NUMBER
  namespace: STUDENT_NAME
spec:
  selector:
    app: tomcat-app-BUILD_NUMBER
  type: ClusterIP
  ports:
  - name: tomcatsvcport
    port: 8080
    targetPort: tomcatport
